apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: metrics-gateway
  namespace: monitoring
  annotations:
    cert-manager.io/cluster-issuer: cloudflare-acme-issuer
spec:
  gatewayClassName: traefik
  listeners:
    - name: websecure
      protocol: HTTPS
      hostname: "metrics.kube.metalnet.org"
      port: 8443
      allowedRoutes:
        namespaces:
          from: Same
      tls:
        mode: Terminate
        certificateRefs:
          - name: metrics-kube-metalnet-org-tls
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: metrics-http-route
  namespace: monitoring
spec:
  parentRefs:
    - name: metrics-gateway
      namespace: monitoring
      sectionName: websecure
  hostnames:
    - "metrics.kube.metalnet.org"
  rules:
    - backendRefs:
        - name: vmsingle-vmks
          port: 8428
          kind: Service
          group: ""
      matches:
        - path:
            type: PathPrefix
            value: /
