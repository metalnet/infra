ingressRoute:
  dashboard:
    enabled: true
    matchRule: Host(`traefik.kube.metalnet.org`)
    entryPoints:
      - web
providers:
  kubernetesGateway:
    enabled: true
gateway:
  listeners:
    web:
      port: 8000
      protocol: HTTP
      namespacePolicy:
        from: All

